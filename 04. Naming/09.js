/*

    SEARCHABLE

    * no single-letter names 
    * exception for iteraters, but don't use l
    * better, use ES6 let..of
   
*/

let list = [8, 3, 11, 9, 6];

for (let value of list) {
    console.log(value);
}
