/*

    FUNCTION ARGUMENTS

    
    * ideal number of arguments is zero
    * one or two are acceptable
    * three or more should be avoided

*/

class Animal {
    constructor(
        species: string,
        mamal: boolean,
        color: string,
        name: string,
        pet: boolean
    ) {}
}

let skunk = new Animal('skunk', true, 'black and white', 'Pepé Le Pew', false);
